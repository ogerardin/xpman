<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.ogerardin.xplane</groupId>
        <artifactId>xpman</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>xpman-fx-dist</artifactId>
    <packaging>jar</packaging>
    <description>
        This module packages the app in forms suitable for distribution.
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <app.mainClass>com.ogerardin.xpman.XPmanFX</app.mainClass>
        <app.runnableJar>${project.build.finalName}-repackaged.jar</app.runnableJar>
        <app.runnableMainClass>org.springframework.boot.loader.JarLauncher</app.runnableMainClass>
        <app.copyright>(c) Olivier Gérardin 2022-2026</app.copyright>
        <app.vendor>Olivier Gérardin</app.vendor>
        <app.name>X-Plane Manager</app.name>
        <app.shortName>xpman</app.shortName>
        <app.description>Configuration Manager for Laminar Research X-Plane</app.description>
        <!--Default when not available from git-commit-id-maven-plugin-->
        <git.build.number>0</git.build.number>
        <!-- jpackage requires a dotted numeric version (1-3 integers, no -SNAPSHOT suffix) -->
        <jpackage.version>${parsedVersion.majorVersion}.${parsedVersion.minorVersion}.${parsedVersion.incrementalVersion}</jpackage.version>
        <!-- xpman-fx-dist adds no third-party dependencies of its own (only xpman-fx), so its
            dependency-check scan would be a strict subset of xpman-fx's: skip it here. -->
        <dependency-check.skip>true</dependency-check.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.ogerardin.xplane</groupId>
            <artifactId>xpman-fx</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>4.1.0</version>
                </plugin>
                <plugin>
                    <groupId>org.panteleyev</groupId>
                    <artifactId>jpackage-maven-plugin</artifactId>
                    <version>1.8.0</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>3.0.0</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>3.5.0</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>

            <!--Extracts icon.png from the xpman-fx dependency-->
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>extract-icon</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>com.ogerardin.xplane</groupId>
                                    <artifactId>xpman-fx</artifactId>
                                    <version>1.0-SNAPSHOT</version>
                                    <type>jar</type>
                                    <includes>img/icon.png</includes>
                                    <outputDirectory>${project.build.directory}</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Generates an uber-jar by method of repackaging using Spring's spring-boot-maven-plugin.
                Much cleaner than shading, the dependencies are not unpacked and merged, but embedded as is, and
                a custom classloader takes care of accessing them. -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>generate-repackaged-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                        <configuration>
                            <!-- <finalName>XPman-${project.version}</finalName> -->
                            <mainClass>${app.mainClass}</mainClass>
                            <classifier>repackaged</classifier>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!--Extracts version components into properties "parsedVersion.*"-->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>parse-version</id>
                        <goals>
                            <goal>parse-version</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

    <profiles>

        <!-- This profile generates a macOS app with bundled JRE, a pkg installer and a dmg image
            for drag-and-drop installation using jpackage -->
        <profile>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <id>jpackage-mac</id>
            <build>
                <plugins>
                    <!--Convert PNG icon (extracted from xpman-fx) to .icns format
                        Uses sips which is a macOS native command-->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>convert-icon-to-icns</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>sips</executable>
                                    <arguments>
                                        <argument>-s</argument>
                                        <argument>format</argument>
                                        <argument>icns</argument>
                                        <argument>${project.build.directory}/img/icon.png</argument>
                                        <argument>--out</argument>
                                        <argument>${project.build.directory}/img/icon.icns</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- jpackage: generates .app, .dmg, and .pkg -->

                    <!-- Prepare jpackage input directory with just the repackaged JAR -->
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>copy-jpackage-input</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>${app.runnableJar}</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <executions>
                            <!-- Build the application image (.app) first -->
                            <execution>
                                <id>jpackage-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>APP_IMAGE</type>
                                    <name>${app.name}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <input>${project.build.directory}/jpackage-input</input>
                                    <mainJar>${app.runnableJar}</mainJar>
                                    <mainClass>${app.runnableMainClass}</mainClass>
                                    <icon>${project.build.directory}/img/icon.icns</icon>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <javaOptions>
                                        <javaOption>--add-opens=javafx.graphics/javafx.scene=org.controlsfx.controls</javaOption>
                                    </javaOptions>
                                    <macAppCategory>utilities</macAppCategory>
                                    <macPackageIdentifier>com.ogerardin.xpman</macPackageIdentifier>
                                    <macPackageName>XPman</macPackageName>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                            <!-- Build DMG from the app image -->
                            <execution>
                                <id>jpackage-dmg</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>DMG</type>
                                    <name>${app.name}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <licenseFile>${project.basedir}/../LICENSE</licenseFile>
                                    <appImage>${project.build.directory}/${app.name}.app</appImage>
                                    <resourceDir>${project.basedir}/assets/mac/jpackage</resourceDir>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                            <!-- Build PKG from the app image -->
                            <execution>
                                <id>jpackage-pkg</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>PKG</type>
                                    <name>${app.name}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <licenseFile>${project.basedir}/../LICENSE</licenseFile>
                                    <appImage>${project.build.directory}/${app.name}.app</appImage>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- This profile generates a Windows installer (exe), MSI, and app image using jpackage -->
        <profile>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <id>jpackage-win</id>
            <build>
                <plugins>
                    <!--Convert PNG icon (extracted from xpman-fx) to .ico format
                        Requires: imagemagick -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>convert-icon-to-ico</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>magick</executable>
                                    <arguments>
                                        <argument>convert</argument>
                                        <argument>${project.build.directory}/img/icon.png</argument>
                                        <argument>-define</argument>
                                        <argument>icon:auto-resize=256,128,96,64,48,32,24,16</argument>
                                        <argument>${project.build.directory}/img/icon.ico</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Prepare jpackage input directory with just the repackaged JAR -->
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>copy-jpackage-input</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>${app.runnableJar}</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- jpackage: generates app image, exe (Inno Setup), and msi (WiX) -->
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <executions>
                            <!-- Build the application image -->
                            <execution>
                                <id>jpackage-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>APP_IMAGE</type>
                                    <name>${app.name}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <input>${project.build.directory}/jpackage-input</input>
                                    <mainJar>${app.runnableJar}</mainJar>
                                    <mainClass>${app.runnableMainClass}</mainClass>
                                    <icon>${project.build.directory}/img/icon.ico</icon>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <javaOptions>
                                        <javaOption>--add-opens=javafx.graphics/javafx.scene=org.controlsfx.controls</javaOption>
                                    </javaOptions>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                            <!-- Build EXE installer using Inno Setup -->
                            <execution>
                                <id>jpackage-exe</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>EXE</type>
                                    <name>${app.name}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <licenseFile>${project.basedir}/../LICENSE</licenseFile>
                                    <appImage>${project.build.directory}/${app.name}</appImage>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <winDirChooser>true</winDirChooser>
                                    <winMenu>true</winMenu>
                                    <winMenuGroup>X-Plane Manager</winMenuGroup>
                                    <winShortcut>true</winShortcut>
                                    <winShortcutPrompt>true</winShortcutPrompt>
                                    <winUpgradeUuid>40f06d94-b1e3-4b20-b127-ad46b892f076</winUpgradeUuid>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                            <!-- Build MSI installer using WiX Toolset -->
                            <execution>
                                <id>jpackage-msi</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>MSI</type>
                                    <name>${app.name}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <licenseFile>${project.basedir}/../LICENSE</licenseFile>
                                    <appImage>${project.build.directory}/${app.name}</appImage>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <winDirChooser>true</winDirChooser>
                                    <winMenu>true</winMenu>
                                    <winMenuGroup>X-Plane Manager</winMenuGroup>
                                    <winShortcut>true</winShortcut>
                                    <winUpgradeUuid>40f06d94-b1e3-4b20-b127-ad46b892f076</winUpgradeUuid>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- This profile generates a Linux app with bundled JRE, a deb package and a rpm package using jpackage -->
        <profile>
            <activation>
                <os>
                    <family>linux</family>
                </os>
            </activation>
            <id>jpackage-linux</id>
            <build>
                <plugins>
                    <!-- Prepare jpackage input directory with just the repackaged JAR -->
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>copy-jpackage-input</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>${app.runnableJar}</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- jpackage: generates .app image, .deb, and .rpm -->
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <executions>
                            <!-- Build the application image -->
                            <execution>
                                <id>jpackage-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>APP_IMAGE</type>
                                    <name>${app.shortName}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <input>${project.build.directory}/jpackage-input</input>
                                    <mainJar>${app.runnableJar}</mainJar>
                                    <mainClass>${app.runnableMainClass}</mainClass>
                                    <icon>${project.build.directory}/img/icon.png</icon>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <javaOptions>
                                        <javaOption>--add-opens=javafx.graphics/javafx.scene=org.controlsfx.controls</javaOption>
                                    </javaOptions>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                            <!-- Build DEB from the app image -->
                            <execution>
                                <id>jpackage-deb</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>DEB</type>
                                    <name>${app.shortName}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <licenseFile>${project.basedir}/../LICENSE</licenseFile>
                                    <appImage>${project.build.directory}/${app.shortName}</appImage>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <linuxPackageName>${app.shortName}</linuxPackageName>
                                    <linuxShortcut>true</linuxShortcut>
                                    <linuxMenuGroup>Games</linuxMenuGroup>
                                    <linuxAppCategory>Games</linuxAppCategory>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                            <!-- Build RPM from the app image -->
                            <execution>
                                <id>jpackage-rpm</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>RPM</type>
                                    <name>${app.shortName}</name>
                                    <appVersion>${jpackage.version}</appVersion>
                                    <vendor>${app.vendor}</vendor>
                                    <description>${app.description}</description>
                                    <copyright>${app.copyright}</copyright>
                                    <licenseFile>${project.basedir}/../LICENSE</licenseFile>
                                    <appImage>${project.build.directory}/${app.shortName}</appImage>
                                    <destination>${project.build.directory}</destination>
                                    <removeDestination>false</removeDestination>
                                    <linuxPackageName>${app.shortName}</linuxPackageName>
                                    <linuxShortcut>true</linuxShortcut>
                                    <linuxMenuGroup>Games</linuxMenuGroup>
                                    <linuxAppCategory>Games</linuxAppCategory>
                                    <linuxRpmLicenseType>GPLv3</linuxRpmLicenseType>
                                    <verbose>true</verbose>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

    </profiles>

</project>